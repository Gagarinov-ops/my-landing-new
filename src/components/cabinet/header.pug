header(class="flex flex-col sm:flex-row justify-between items-center gap-4 mb-8")
  h1(class="text-3xl font-bold text-[var(--text-primary)]") Личный кабинет
  button(
    id="logout-btn",
    class="bg-red-500 text-white px-4 py-2 rounded hover:opacity-90 transition focus:outline-none focus:ring-2 focus:ring-red-700 focus:ring-offset-2 focus:ring-offset-[var(--bg-primary)]",
    aria-label="Выйти из аккаунта"
  ) Выйти